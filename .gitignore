.codegraph/

# LimboAI GDExtension（~32MB 各平台二进制，不入库）
# 装法见 CLAUDE.md「LimboAI 依赖」：从 AssetLib 或 GitHub release 装 v1.7.1 (gdextension-4.6)
addons/limboai/

# 工具 / 临时 / 可再生产物，不入库
.claude/
.superpowers/
.playwright-mcp/
.tmp_limboai/
reports/
telemetry/
.montage_*.png
.montage_*.png.import

# BGM：署名要求 + 体积大，本地放置(见 CLAUDE.md)
assets/audio/music/

# Godot 编辑器易变状态/UI 缓存：每次开编辑器都重生成，不入库（曾混入 977+ 文件噪音）。
# clone 即跑所需的 .godot/imported、shader_cache、global_script_class_cache.cfg 仍保留追踪。
.godot/editor/
.godot/uid_cache.bin
